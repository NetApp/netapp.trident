---
galaxy_info:
  role_name: create_storageclass
  namespace: netapp
  author: Kamini Singh
  description: Create Kubernetes StorageClasses backed by NetApp Trident (NFS, NFS FlexGroup, iSCSI, NVMe/TCP, FCP).
  company: NetApp
  license: Apache-2.0
  min_ansible_version: "2.16"
  galaxy_tags:
    - netapp
    - trident
    - openshift
    - kubernetes
    - storage
    - storageclass

dependencies: []
  # List your role dependencies here, one per line. Be sure to remove the '[]' above,
  # if you add dependencies to this list.
