---
galaxy_info:
  role_name: delete_pvcs
  namespace: netapp
  author: Kamini Singh
  description: Delete PersistentVolumeClaims and the namespace used for provisioning.
  company: NetApp
  license: Apache-2.0
  min_ansible_version: "2.16"
  galaxy_tags:
    - netapp
    - trident
    - openshift
    - kubernetes
    - storage
    - pvc

dependencies: []
  # List your role dependencies here, one per line. Be sure to remove the '[]' above,
  # if you add dependencies to this list.
