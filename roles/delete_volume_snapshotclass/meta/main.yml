---
galaxy_info:
  role_name: delete_volume_snapshotclass
  namespace: netapp
  author: Kamini Singh
  description: Delete a VolumeSnapshotClass from a Kubernetes cluster.
  company: NetApp
  license: Apache-2.0
  min_ansible_version: "2.16"
  galaxy_tags:
    - netapp
    - trident
    - openshift
    - kubernetes
    - storage
    - snapshot

dependencies: []
  # List your role dependencies here, one per line. Be sure to remove the '[]' above,
  # if you add dependencies to this list.
