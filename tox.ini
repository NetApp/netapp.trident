[tox]
envlist=flake8,yamllint,ansible-lint

[testenv:lint]
labels = lint
description = run linting checks
deps = flake8
commands = flake8 --max-line-length=160 --exclude=.ansible,.git,.tox,.venv,venv,build,dist,changelogs,tests/output .

[testenv:yamllint]
labels = lint
description = run yamllint on YAML sources
deps = yamllint
commands = yamllint .

[testenv:ansible-lint]
labels = lint
description = run ansible-lint on the collection
deps =
    ansible-lint
    ansible-core>=2.16
commands = ansible-lint
